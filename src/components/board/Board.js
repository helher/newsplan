// Dependencies
import DragList from "./DragList";

// Styling
import './Board.scss';

function Board() {
    return (
        <section className="board">
            <DragList />
        </section>
    );
};

export default Board;